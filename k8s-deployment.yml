apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: react-flask-app
  name: react-flask-app
  labels:
    app: react-flask-app
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: react-flask-app
  template:
    metadata:
      labels:
        app: react-flask-app
    spec:
      serviceAccountName: react-flask-sa
      containers:
        - name: flask-backend
          image: nomanakram29/reactjs-flask-backend:latest
          ports:
            - containerPort: 5000
          env:
            - name: FLASK_ENV
              value: production
            - name: JWT_SECRET_KEY
              value: super-secret
            - name: SECRET_KEY
              value: super-secret
          imagePullPolicy: Always
        - name: react-frontend
          image: nomanakram29/reactjs-flask-frontend:latest
          ports:
            - containerPort: 5173
          imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  namespace: react-flask-app
  name: flask-backend-service
spec:
  selector:
    app: react-flask-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  namespace: react-flask-app
  name: react-frontend-service
spec:
  selector:
    app: react-flask-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5173
  type: LoadBalancer
